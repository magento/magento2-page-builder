<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../../../../../../../dev/tests/acceptance/vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="ActivatePageBuilderFullScreen">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-71798: Bluefoot in full screen mode"/>
            <title value="Activate PageBuilder CMS in fullscreen mode and then unactivate it."/>
            <description value="You should be able to activate PageBuilder FullScreen in Admin."/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-81951"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <waitForElementVisible selector="{{PageBuilderActionsSection.stageWrapper}}" stepKey="waitForStage"/>
        <waitForElementVisible selector="{{PageBuilderActionsSection.openFullScreen}}" stepKey="waitForFullScreenButton"/>
        <waitForElementNotVisible selector="{{PageBuilderActionsSection.stageWrapperFullScreen}}" stepKey="verifyStageNotFullScreen"/>
        <click selector="{{PageBuilderActionsSection.openFullScreen}}" stepKey="clickFullScreenButton"/>
        <waitForElementVisible selector="{{PageBuilderActionsSection.stageWrapperFullScreen}}" stepKey="waitForStageFullScreen"/>
        <waitForElementVisible selector="{{PageBuilderActionsSection.exitFullScreen}}" stepKey="waitForExitFullScreenButton"/>
        <waitForElementNotVisible selector="{{PageBuilderActionsSection.stageWrapper}}" stepKey="verifyStageNotNormalScreen"/>
        <click selector="{{PageBuilderActionsSection.exitFullScreen}}" stepKey="clickExitFullScreen"/>
        <waitForElementVisible selector="{{PageBuilderActionsSection.stageWrapper}}" stepKey="waitForStage2"/>
        <waitForElementNotVisible selector="{{PageBuilderActionsSection.stageWrapperFullScreen}}" stepKey="verifyStageNotFullScreen2"/>
    </test>
    <test name="ShowPanelGroups">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-71566: Filtering Bluefoot content elements on the Panel"/>
            <title value="Display Existing PageBuilder Panel Content Block Groups"/>
            <description value="The PageBuilder panel should display existing content block groups, and open the first section by default."/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-81826"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <seeElement selector='{{PageBuilderActionsSection.firstSection}}' parameterArray="['class' => 'active']" stepKey="firstSectionOfPanelIsOpen"/>
        <actionGroup ref="validateGroupsAndContentTypes" stepKey="validateGroupsAndContentTypes"/>
    </test>
    <test name="SearchOneCharInPanel">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-71566: Filtering Bluefoot content elements on the Panel"/>
            <title value="Search for one character in PageBuilder Panel"/>
            <description value="When typing one letter into the panel search box, I see Content Blocks that contains words that start with letter entered."/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-81827"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="b" stepKey="fillSearch"/>
        <see userInput="Buttons" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateBlockButtons"/>
        <see userInput="Banner" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateBlockBanner"/>
        <see userInput="Block" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateBlockStaticBlock"/>
    </test>
    <test name="SearchRemovedContentBlock">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-66517: Bluefoot Search Blocks removal"/>
            <title value="Search for removed content block on PageBuilder Panel"/>
            <description value="When typing Search/Code into the panel search box, I shouldn't see Code/Search Content Blocks displays"/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-88637"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="Search" stepKey="fillSearch"/>
        <dontSee userInput="Search" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateSearchButtons"/>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="Code" stepKey="fillCode"/>
        <dontSee userInput="Code" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateCodeButtons"/>
    </test>
    <test name="SearchThreeCharInPanel">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-71566: Filtering Bluefoot content elements on the Panel"/>
            <title value="Search for three characters in PageBuilder Panel"/>
            <description value="When typing three letters into the panel search box, I see content blocks that contains words that start with letters entered."/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-81827"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="blo" stepKey="fillSearch"/>
        <see userInput="Block" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateBlockButtons"/>
    </test>
    <test name="SearchNoMatchesInPanel">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-71566: Filtering Bluefoot content elements on the Panel"/>
            <title value="Search for no matches in PageBuilder Panel"/>
            <description value="When there is no match on a search, display appropriate message."/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-81831"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="abcdef" stepKey="fillSearch"/>
        <see userInput="Nothing found" selector="{{PageBuilderActionsSection.nothingFound}}" stepKey="validateNothingFound"/>
    </test>
    <test name="ClearSearchInPanel">
        <annotations>
            <features value="Pagebuilder: Panel"/>
            <stories value="MAGETWO-71566: Filtering Bluefoot content elements on the Panel"/>
            <title value="Clear search in PageBuilder Panel using clear search button and backspace"/>
            <description value="The clear search button is displayed only when I start typing in the search field. When a search is cleared, content block groups are displayed."/>
            <severity value="CRITICAL"/>
            <testCaseId value="MAGETWO-81828"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <comment userInput="Verify Clear Search button is not shown before typing" stepKey="commentClearSearchButton"/>
        <dontSeeElement selector='//*[@id="pagebuilder-panel"]/div[1]/a' stepKey="dontSeeClearSearch"/>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="i" stepKey="fillSearchFieldi"/>
        <seeElement selector='{{PageBuilderActionsSection.searchField}}' stepKey="seeClearSearch"/>
        <see userInput="Image" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateImageAppears"/>
        <dontSee userInput="Row" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateRowDoesNotAppear"/>
        <comment userInput="Use Clear Search button to show all content blocks on Pagebuilder panel" stepKey="commentClearSearch"/>
        <click selector="{{PageBuilderActionsSection.clearSearch}}" stepKey="clickClearSearch"/>
        <dontSee userInput="i" selector="{{PageBuilderActionsSection.searchPanel}}" stepKey="validateInputClearedAfterClickClear"/>
        <actionGroup ref="validateGroupsAndContentTypes" stepKey="validateGroupsAndContentTypesAfterClickClear"/>
        <actionGroup ref="closePageBuilderPanelGroups" stepKey="closePageBuilderPanelGroups"/>
        <fillField selector="{{PageBuilderActionsSection.searchPanel}}" userInput="r" stepKey="fillSearchFieldr"/>
        <see userInput="Row" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateRowAppears"/>
        <dontSee userInput="Column" selector="{{PageBuilderActionsSection.searchResults}}" stepKey="validateColumnDoesNotAppear"/>
        <comment userInput="Clear input field using backspace to show all content blocks on Pagebuilder panel" stepKey="commentDeleteSearch"/>
        <pressKey selector="{{PageBuilderActionsSection.searchPanel}}" parameterArray="[\WebDriverKeys::BACKSPACE]" stepKey="enterSearchInput"/>
        <dontSee userInput="r" selector="{{PageBuilderActionsSection.searchPanel}}" stepKey="validateInputClearedAfterRemoveCharacters"/>
        <actionGroup ref="validateGroupsAndContentTypes" stepKey="validateGroupsAndContentTypesAfterClearField"/>
    </test>
</tests>
