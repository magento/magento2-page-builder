<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="../../../../../../../dev/tests/acceptance/vendor/magento/magento2-functional-testing-framework/src/Magento/FunctionalTestingFramework/Test/etc/testSchema.xsd">
    <test name="ActivatePageBuilderFullScreen">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Activate PageBuilder CMS in fullscreen mode and then unactivate it."/>
            <description value="You should be able to activate PageBuilder FullScreen in Admin."/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-71798"/>
            <testCaseId value="MAGETWO-81951"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <waitForElementVisible selector="{{PageBuilderStage.stageWrapper}}" stepKey="waitForStage"/>
        <waitForElementVisible selector="{{PageBuilderStage.openFullScreen}}" stepKey="waitForFullScreenButton"/>
        <waitForElementNotVisible selector="{{PageBuilderStage.stageWrapperFullScreen}}" stepKey="verifyStageNotFullScreen"/>
        <click selector="{{PageBuilderStage.openFullScreen}}" stepKey="clickFullScreenButton"/>
        <waitForElementVisible selector="{{PageBuilderStage.stageWrapperFullScreen}}" stepKey="waitForStageFullScreen"/>
        <waitForElementVisible selector="{{PageBuilderStage.exitFullScreen}}" stepKey="waitForExitFullScreenButton"/>
        <waitForElementNotVisible selector="{{PageBuilderStage.stageWrapper}}" stepKey="verifyStageNotNormalScreen"/>
        <click selector="{{PageBuilderStage.exitFullScreen}}" stepKey="clickExitFullScreen"/>
        <waitForElementVisible selector="{{PageBuilderStage.stageWrapper}}" stepKey="waitForStage2"/>
        <waitForElementNotVisible selector="{{PageBuilderStage.stageWrapperFullScreen}}" stepKey="verifyStageNotFullScreen2"/>
    </test>
    <test name="ShowPanelGroups">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Display Existing PageBuilder Panel Content Type Groups"/>
            <description value="The PageBuilder panel should display existing content type groups, and open the first section by default."/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-71566"/>
            <testCaseId value="MAGETWO-81826"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <seeElement selector='{{PageBuilderPanel.firstSection}}' parameterArray="['class' => 'active']" stepKey="firstSectionOfPanelIsOpen"/>
        <actionGroup ref="validateGroupsAndContentTypes" stepKey="validateGroupsAndContentTypes"/>
    </test>
    <test name="SearchOneCharInPanel">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Search for one character in PageBuilder Panel"/>
            <description value="When typing one letter into the panel search box, I see Content Types that contains words that start with letter entered."/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-71566"/>
            <testCaseId value="MAGETWO-81827"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="b" stepKey="fillSearch"/>
        <see userInput="Buttons" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateContentTypeButtons"/>
        <see userInput="Banner" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateContentTypeBanner"/>
        <see userInput="Block" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateContentTypeStaticBlock"/>
    </test>
    <test name="SearchRemovedContentType">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Search for removed content type on PageBuilder Panel"/>
            <description value="When typing Search/Code into the panel search box, I shouldn't see Code/Search Content Types displays"/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-66517"/>
            <testCaseId value="MAGETWO-88637"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="Search" stepKey="fillSearch"/>
        <dontSee userInput="Search" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateSearchButtons"/>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="Code" stepKey="fillCode"/>
        <see userInput="HTML Code" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateCodeButtons"/>
    </test>
    <test name="SearchThreeCharInPanel">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Search for three characters in PageBuilder Panel"/>
            <description value="When typing three letters into the panel search box, I see content types that contains words that start with letters entered."/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-71566"/>
            <testCaseId value="MAGETWO-81827"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="blo" stepKey="fillSearch"/>
        <see userInput="Block" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateStaticBlock"/>
    </test>
    <test name="SearchNoMatchesInPanel">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Search for no matches in PageBuilder Panel"/>
            <description value="When there is no match on a search, display appropriate message."/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-71566"/>
            <testCaseId value="MAGETWO-81831"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="abcdef" stepKey="fillSearch"/>
        <see userInput="Nothing found" selector="{{PageBuilderPanel.nothingFound}}" stepKey="validateNothingFound"/>
    </test>
    <test name="ClearSearchInPanel">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Clear search in PageBuilder Panel using clear search button and backspace"/>
            <description value="The clear search button is displayed only when I start typing in the search field. When a search is cleared, content type groups are displayed."/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-71566"/>
            <testCaseId value="MAGETWO-81828"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <comment userInput="Verify Clear Search button is not shown before typing" stepKey="commentClearSearchButton"/>
        <dontSeeElement selector='//*[@id="pagebuilder-panel"]/div[1]/a' stepKey="dontSeeClearSearch"/>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="i" stepKey="fillSearchFieldi"/>
        <seeElement selector='{{PageBuilderPanel.searchField}}' stepKey="seeClearSearch"/>
        <see userInput="Image" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateImageAppears"/>
        <dontSee userInput="Row" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateRowDoesNotAppear"/>
        <comment userInput="Use Clear Search button to show all content types on Pagebuilder panel" stepKey="commentClearSearch"/>
        <click selector="{{PageBuilderPanel.clearSearch}}" stepKey="clickClearSearch"/>
        <dontSee userInput="i" selector="{{PageBuilderPanel.searchPanel}}" stepKey="validateInputClearedAfterClickClear"/>
        <actionGroup ref="validateGroupsAndContentTypes" stepKey="validateGroupsAndContentTypesAfterClickClear"/>
        <actionGroup ref="closePageBuilderPanelGroups" stepKey="closePageBuilderPanelGroups"/>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="r" stepKey="fillSearchFieldr"/>
        <see userInput="Row" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateRowAppears"/>
        <dontSee userInput="Column" selector="{{PageBuilderPanel.searchResults}}" stepKey="validateColumnDoesNotAppear"/>
        <comment userInput="Clear input field using backspace to show all content types on Pagebuilder panel" stepKey="commentDeleteSearch"/>
        <pressKey selector="{{PageBuilderPanel.searchPanel}}" parameterArray="[\WebDriverKeys::BACKSPACE]" stepKey="enterSearchInput"/>
        <dontSee userInput="r" selector="{{PageBuilderPanel.searchPanel}}" stepKey="validateInputClearedAfterRemoveCharacters"/>
        <scrollTo selector="{{CmsNewPagePageActionsSection.contentSectionName}}" stepKey="scrollToTopOfStage"/>
        <actionGroup ref="validateGroupsAndContentTypes" stepKey="validateGroupsAndContentTypesAfterClearField"/>
    </test>
    <test name="SearchContentTypeAndAddToStage">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Search for content blocks and add them to stage"/>
            <description value="After search I can drag and drop content blocks onto the stage before and after clicking X on the panel search "/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-92004"/>
            <testCaseId value="MAGETWO-92386"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <comment userInput="Search Map in Panel and drag to stage" stepKey="commentSearchMap"/>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="Map" stepKey="fillSearchField"/>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragContentTypeToStage">
            <argument name="contentType" value="PageBuilderMapContentType"/>
        </actionGroup>
        <actionGroup ref="closeEditPanelSettings" stepKey="closeEditPanelSettings"/>
        <comment userInput="Clear search Map and drag Column to stage" stepKey="commentClearSearch"/>
        <waitForElementVisible selector='{{PageBuilderPanel.searchField}}' stepKey="seeClearSearch"/>
        <click selector='{{PageBuilderPanel.searchField}}' stepKey="clickClearSearch"/>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragColumnToStage">
            <argument name="contentType" value="PageBuilderColumnContentType"/>
        </actionGroup>
        <dontSeeJsError stepKey="dontSeeJsError"/>
    </test>
    <test name="PanelExpandAndCollapseAndAddToStage">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Collapse and Expand search panel and still able to search and add to stage"/>
            <description value="After expand and collapse search panel I can search and drop content blocks onto the stage"/>
            <severity value="CRITICAL"/>
            <useCaseId value="MAGETWO-92004"/>
            <testCaseId value="MAGETWO-92470"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragColumnToStage">
            <argument name="contentType" value="PageBuilderColumnContentType"/>
        </actionGroup>
        <comment userInput="Collapse and Expand panel then drag Tabs to Column" stepKey="commentDragTabs"/>
        <waitForElementVisible selector="{{PageBuilderStage.stageWrapper}}" stepKey="waitForStage"/>
        <waitForElementVisible selector="{{PageBuilderPanel.collapsePanel}}" stepKey="waitForCollapseButton"/>
        <click selector="{{PageBuilderPanel.collapsePanel}}" stepKey="clickCollapseButton"/>
        <waitForElementVisible selector="{{PageBuilderPanel.expandPanel}}" stepKey="waitForExpandButton"/>
        <waitForElementVisible selector="{{PageBuilderPanel.panelCollapsed}}" stepKey="waitForPanelCollapsed"/>
        <waitForElementNotVisible selector="{{PageBuilderPanel.collapsePanel}}" stepKey="waitForCollapsePanelButtonNotVisible"/>
        <waitForElementNotVisible selector="{{PageBuilderPanel.searchPanel}}" stepKey="waitForSearchFieldNotVisible"/>
        <click selector="{{PageBuilderPanel.expandPanel}}" stepKey="clickExpandButton"/>
        <waitForElementNotVisible selector="{{PageBuilderPanel.panelCollapsed}}" stepKey="waitForPanelCollapsedNotVisible"/>
        <waitForElementNotVisible selector="{{PageBuilderPanel.expandPanel}}" stepKey="waitForExpandButtonNotVisible"/>
        <waitForElementVisible selector="{{PageBuilderPanel.collapsePanel}}" stepKey="waitForCollapseButtonVisible"/>
        <waitForElementVisible selector="{{PageBuilderPanel.searchPanel}}" stepKey="waitForSearchField"/>
        <fillField selector="{{PageBuilderPanel.searchPanel}}" userInput="ta" stepKey="fillSearchFieldTa"/>
        <actionGroup ref="dragContentTypeToContainer" stepKey="dragTabsIntoFirstColumn">
            <argument name="contentType" value="PageBuilderTabsContentType"/>
            <argument name="containerTargetType" value="PageBuilderColumnContentType"/>
            <argument name="containerTargetIndex" value="1"/>
        </actionGroup>
        <dontSeeJsError stepKey="dontSeeJsError"/>
    </test>
    <test name="ValidatePanelContentTypesGroupedCorrectly">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Content types are grouped as specified"/>
            <description value="The panel should contain the proper groups with content types"/>
            <severity value="MINOR"/>
            <useCaseId value="MC-943"/>
            <testCaseId value="MC-2897"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <!-- Validate Layout group-->
        <comment userInput="Validate layout group" stepKey="commentValidatePanel1"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('1', 'Layout', '1', 'Row')}}" stepKey="validateContentType1"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('1', 'Layout', '2', 'Column')}}" stepKey="validateContentType2"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('1', 'Layout', '3', 'Tabs')}}" stepKey="validateContentType3"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('1', 'Layout', '4', 'Accordion')}}" stepKey="validateContentType4"/>
        <click selector="{{PageBuilderPanel.panelGroup('Layout')}}" stepKey="closeLayoutGroup"/>
        <!-- Validate Elements group -->
        <comment userInput="Validate elements group" stepKey="commentValidatePanel2"/>
        <click selector="{{PageBuilderPanel.panelGroup('Elements')}}" stepKey="openElementGroup"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '1', 'Text')}}" stepKey="validateContentType5"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '2', 'Heading')}}" stepKey="validateContentType6"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '3', 'Buttons')}}" stepKey="validateContentType7"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '4', 'Divider')}}" stepKey="validateContentType8"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '5', 'Newsletter')}}" stepKey="validateContentType9"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '6', 'Anchor')}}" stepKey="validateContentType10"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('2', 'Elements', '7', 'HTML Code')}}" stepKey="validateContentType11"/>
        <click selector="{{PageBuilderPanel.panelGroup('Elements')}}" stepKey="closeElementGroup"/>
        <!-- Validate Media Group -->
        <comment userInput="Validate media group" stepKey="commentValidatePanel3"/>
        <click selector="{{PageBuilderPanel.panelGroup('Media')}}" stepKey="openMediaGroup"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('3', 'Media', '1', 'Image')}}" stepKey="validateContentType12"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('3', 'Media', '2', 'Video')}}" stepKey="validateContentType13"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('3', 'Media', '3', 'Banner')}}" stepKey="validateContentType14"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('3', 'Media', '4', 'Slider')}}" stepKey="validateContentType15"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('3', 'Media', '5', 'Map')}}" stepKey="validateContentType16"/>
        <click selector="{{PageBuilderPanel.panelGroup('Media')}}" stepKey="closeMediaGroup"/>
        <!-- Validate Add Content Group -->
        <comment userInput="Validate add content group" stepKey="commentValidatePanel4"/>
        <click selector="{{PageBuilderPanel.panelGroup('Add Content')}}" stepKey="openAddContentGroup"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('4', 'Add Content', '1', 'Block')}}" stepKey="validateContentType17"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeInGroup('4', 'Add Content', '2', 'Products')}}" stepKey="validateContentType18"/>
        <click selector="{{PageBuilderPanel.panelGroup('Add Content')}}" stepKey="closeAddContentGroup"/>
    </test>
    <test name="ValidatePanelDefaultState">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Panel default state as specified"/>
            <description value="The panel should only have the layout group expanded"/>
            <severity value="MINOR"/>
            <useCaseId value="MC-943"/>
            <testCaseId value="MC-2897"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <seeElement selector="{{PageBuilderPanel.panelGroupActive('Layout')}}" stepKey="checkLayoutActive"/>
        <dontSeeElement selector="{{PageBuilderPanel.panelGroupActive('Elements')}}" stepKey="checkInactiveGroup1"/>
        <dontSeeElement selector="{{PageBuilderPanel.panelGroupActive('Media')}}" stepKey="checkInactiveGroup2"/>
        <dontSeeElement selector="{{PageBuilderPanel.panelGroupActive('Add Content')}}" stepKey="checkInactiveGroup3"/>
    </test>
    <test name="ValidatePanelContentTypeIcons">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Panel Icons are as specified"/>
            <description value="The panel should have correct icons for each content type"/>
            <severity value="MINOR"/>
            <useCaseId value="MC-943"/>
            <testCaseId value="MC-3199"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <!-- Validate Layout Icons -->
        <comment userInput="Validate layout icons" stepKey="commentValidatePanel1"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('row','Row')}}" stepKey="validateIcon1"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('column','Column')}}" stepKey="validateIcon2"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('tabs','Tabs')}}" stepKey="validateIcon3"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('accordion','Accordion')}}" stepKey="validateIcon4"/>
        <click selector="{{PageBuilderPanel.panelGroup('Layout')}}" stepKey="closeLayoutGroup"/>
        <!-- Validate Elements Icons -->
        <comment userInput="Validate elements icons" stepKey="commentValidatePanel2"/>
        <click selector="{{PageBuilderPanel.panelGroup('Elements')}}" stepKey="openElementsGroup"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('text','Text')}}" stepKey="validateIcon5"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('heading','Heading')}}" stepKey="validateIcon6"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('button','Buttons')}}" stepKey="validateIcon7"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('divider','Divider')}}" stepKey="validateIcon8"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('newsletter','Newsletter')}}" stepKey="validateIcon9"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('anchor','Anchor')}}" stepKey="validateIcon10"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('code','HTML Code')}}" stepKey="validateIcon11"/>
        <click selector="{{PageBuilderPanel.panelGroup('Elements')}}" stepKey="closeElementsGroup"/>
        <!-- Validate Media Icons -->
        <comment userInput="Validate media icons" stepKey="commentValidatePanel3"/>
        <click selector="{{PageBuilderPanel.panelGroup('Media')}}" stepKey="openMediaGroup"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('image','Image')}}" stepKey="validateIcon12"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('video','Video')}}" stepKey="validateIcon13"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('banner','Banner')}}" stepKey="validateIcon14"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('slider','Slider')}}" stepKey="validateIcon15"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('map','Map')}}" stepKey="validateIcon16"/>
        <click selector="{{PageBuilderPanel.panelGroup('Media')}}" stepKey="closeMediaGroup"/>
        <!-- Validate Add Content Icons -->
        <comment userInput="Validate add content icons" stepKey="commentValidatePanel4"/>
        <click selector="{{PageBuilderPanel.panelGroup('Add Content')}}" stepKey="openAddContentGroup"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('block','Block')}}" stepKey="validateIcon17"/>
        <seeElement selector="{{PageBuilderPanel.contentTypeWithIcon('products','Products')}}" stepKey="validateIcon18"/>
    </test>
    <test name="HeaderFixedInFullScreen">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Header is fixed in fullscreen"/>
            <description value="The header should be fixed at top after scrolling down"/>
            <severity value="AVERAGE"/>
            <useCaseId value="MC-3168"/>
            <testCaseId value="MC-943"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage1">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage2">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage3">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage4">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage5">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <click selector="{{PageBuilderStage.openFullScreen}}" stepKey="enterFullScreen"/>
        <scrollTo selector="{{RowOnStage.base('6')}}" stepKey="scrollToBottom"/>
        <seeElement selector="{{PageBuilderStage.exitFullScreen}}" stepKey="verifyHeader"/>
    </test>
    <test name="ValidatePanelStickiness">
        <annotations>
            <features value="PageBuilder"/>
            <stories value="Panel"/>
            <title value="Panel should be sticky to the top"/>
            <description value="Panel should remain in view after scrolling down"/>
            <severity value="AVERAGE"/>
            <useCaseId value="MC-943"/>
            <testCaseId value="MC-3200"/>
            <group value="pagebuilder"/>
            <group value="pagebuilder-panel"/>
        </annotations>
        <before>
            <actionGroup ref="LoginAsAdmin" stepKey="loginAsAdmin"/>
            <actionGroup ref="navigateToAPageWithPageBuilder" stepKey="navigateToAPageWithPageBuilder"/>
            <actionGroup ref="switchToPageBuilderStage" stepKey="switchToPageBuilderStage"/>
        </before>
        <after>
            <actionGroup ref="logout" stepKey="logout"/>
        </after>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage1">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage2">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage3">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage4">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <actionGroup ref="dragContentTypeToStage" stepKey="dragRowIntoStage5">
            <argument name="contentType" value="PageBuilderRowContentType"/>
        </actionGroup>
        <scrollTo selector="{{RowOnStage.base('6')}}" stepKey="scrollToBottom1"/>
        <seeElement selector="{{PageBuilderPanel.panelGroup('Layout')}}" stepKey="verifyPanel1"/>
        <scrollToTopOfPage stepKey="scrollToTopOfPage"/>
        <click selector="{{PageBuilderStage.openFullScreen}}" stepKey="enterFullScreen"/>
        <scrollTo selector="{{RowOnStage.base('6')}}" stepKey="scrollToBottom2"/>
        <seeElement selector="{{PageBuilderPanel.panelGroup('Layout')}}" stepKey="verifyPanel2"/>
    </test>
</tests>
